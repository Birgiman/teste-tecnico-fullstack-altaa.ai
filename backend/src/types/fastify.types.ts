import {
  type FastifyError,
  type FastifyReply,
  type FastifyRequest,
} from 'fastify';

export type Req = FastifyRequest;
export type Reply = FastifyReply;
export type FastifyErr = FastifyError;
